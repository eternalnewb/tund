* change tund.rb to allow multiple port forwards
x change install to recognize OS X systems and install the launchdaemon
x get working launchdaemon installed

# NEXT STEPS

x write setup_darwin.sh. beginnings in setup_mess
x write .plist
x figure out how to pass remote.server & other variables to tund 'bin'
* move tundconf.yaml to /etc/tund during setup phases
* parse tundconf.yaml in tund2
* replace tund with tund2
* make debug/status stuff in setup_darwin pretty
